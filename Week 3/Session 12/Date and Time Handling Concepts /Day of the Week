class Solution {
    public String dayOfTheWeek(int day, int month, int year) {
        String[] days = {"Sunday", "Monday", "Tuesday", "Wednesday", "Thursday", "Friday", "Saturday"};
        int[] daysInMonth = {31, 28, 31, 30, 31, 30, 31, 31, 30, 31, 30, 31};
        
        int totalDays = day;
        
        // Add days for all preceding years since 1971
        for (int y = 1971; y < year; y++) {
            totalDays += isLeapYear(y) ? 366 : 365;
        }
        
        // Add days for preceding months in current year
        for (int m = 0; m < month - 1; m++) {
            totalDays += daysInMonth[m];
        }
        
        // Add 1 extra day if current year is leap year and month is past February
        if (month > 2 && isLeapYear(year)) {
            totalDays++;
        }
        
        // Jan 1, 1971 was Friday (index 5)
        return days[(totalDays + 4) % 7];
    }
    
    private boolean isLeapYear(int year) {
        return (year % 4 == 0 && year % 100 != 0) || (year % 400 == 0);
    }
}
